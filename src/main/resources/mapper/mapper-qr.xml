<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-qr">

	<select id="checkVehicle" resultType="int" parameterType="com.team03.Ohfifthlane_back.vo.DisabledPersonVO">
	    SELECT COALESCE(COUNT(disabled_person_id), 0) 
	    FROM ohfifthlane.disabled_person_tbl 
	    WHERE disabled_person_car_num = #{disabledPersonCarNum}
	      AND issue_date >= (CURRENT_DATE - INTERVAL '3 years')
	</select>
   
</mapper>