<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-reservation">

	<!-- 해당 주차구역 예약 생성하기 -->
	<insert id="putReservationByRPZId"
		parameterType="com.team03.Ohfifthlane_back.vo.ReservationVO">

		INSERT INTO reservation_tbl (user_id,
		reservation_start_time,
		reservation_end_time,
		reservation_day,
		reservation_total_fee,
		rpz_id
		)
		VALUES (#{userId},
		#{reservationStartTime},
		#{reservationEndTime},
		#{reservationDay},
		#{reservationTotalFee},
		#{rpzId}
		)
		
	</insert>
	
	<!-- userId로 예약 리스트 가져오기 -->
	<select id="getReservationListByUserId" parameterType="int" resultType="com.team03.Ohfifthlane_back.vo.ReservationVO">
		select * from ohfifthlane.reservation_tbl where user_id = #{userId}
	
	</select>
</mapper>



